import { useId } from 'react';
import { BUtton } from '~/components/button';

export const ThemeToggle = () => {
    return (
        <Button>
            <svg>
                <defs>
                    <circle />
                    <circle />
                </defs>
            </svg>
        </Button>
    );
};